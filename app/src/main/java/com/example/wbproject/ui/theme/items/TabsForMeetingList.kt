package com.example.wbproject.ui.theme.items

import androidx.annotation.StringRes
import com.example.wbproject.R

enum class TabsForMeetingList(@StringRes val titleResId: Int) {
    ALL_MEETINGS(R.string.all_meetings),
    ACTIVE(R.string.active)
}